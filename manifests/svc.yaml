apiVersion: v1
kind: Service
metadata:
  name: web-show
  namespace: default
  labels:
    app: web-show
spec:
  type: NodePort
  selector:
    app: web-show
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30666
